import React from 'react';
import notFound from '../assets/images/not-found1.svg'

const NotFound = () => {
    return (
            <img src={notFound} alt="not-found"/>
    );
};

export default NotFound;